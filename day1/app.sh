mkdir -> mkdir command for create a new directory.
ls -> ls cammand for list files and directories in the current directory.
ls -a -> ls command with the -a option for list all files and directories, including hidden ones.
ls -l -> ls command with the -l option for list files and directories in long format, showing detailed information such as permissions, ownership, size, and modification date.
ls -al -> ls command with the -al options for list all files and directories in long format, including hidden ones.
cd -> cd command for change the current directory.
pwd -> pwd command for print the current working directory.
cat -> cat command for display the contents of a file.
cat > file.txt -> cat command with the > operator for create a new file and write content to it.
cat >> file.txt -> cat command with the >> operator for append content to an existing file.
cat file1.txt file2.txt -> cat command for concatenate the contents of multiple files and display it.
cat file1.txt file2.txt > combined.txt -> cat command with the > operator for concatenate the contents of multiple files and save it to a new file.





# rmdir -> rmdir command for remove an empty directory.
# touch -> touch command for create a new empty file or update the timestamp of an existing file.
# rm -> rm command for remove files or directories.
# cp -> cp command for copy files or directories.
# mv -> mv command for move or rename files or directories.
# echo -> echo command for display a line of text or a variable value.
# grep -> grep command for search for a specific pattern in a file or output.
# find -> find command for search for files and directories based on various criteria.
# chmod -> chmod command for change the permissions of a file or directory.
# chown -> chown command for change the ownership of a file or directory.
# chgrp -> chgrp command for change the group ownership of a file or directory.
# ln -> ln command for create a symbolic link to a file or directory.
# df -> df command for display the disk space usage of file systems.
# du -> du command for display the disk space usage of files and directories.
# ps -> ps command for display information about running processes.
# top -> top command for display real-time information about running processes and system resource usage.
# kill -> kill command for terminate a running process by its PID.
# tar -> tar command for create or extract compressed archive files.
# gzip -> gzip command for compress files using the gzip algorithm.
# gunzip -> gunzip command for decompress files compressed with gzip.
# zip -> zip command for create compressed archive files in ZIP format.
# unzip -> unzip command for extract files from a ZIP archive.
# man -> man command for display the manual pages for a command or utility.
# help -> help command for display a brief description of built-in commands and their usage.
# history -> history command for display a list of previously executed commands.
# alias -> alias command for create a shortcut for a command or a series of commands.
# unalias -> unalias command for remove an alias created with the alias command.
# which -> which command for display the path of an executable command.
# whereis -> whereis command for locate the binary, source, and manual files for a command.
# whoami -> whoami command for display the current user's username.
# id -> id command for display the user ID and group ID of the current user.
# uname -> uname command for display system information such as the kernel name, version, and architecture.
# uptime -> uptime command for display how long the system has been running and the load average.
# date -> date command for display the current date and time.
# cal -> cal command for display a calendar for a specific month and year.
# time -> time command for measure the execution time of a command or script.
# df -h -> df command with the -h option for display disk space usage in a human-readable format.
# du -h -> du command with the -h option for display disk space usage of files and directories in a human-readable format.
# ps aux -> ps command with the aux options for display detailed information about all running processes.
# top -b -> top command with the -b option for display real-time information about running processes and system resource usage in batch mode.
# kill -9 -> kill command with the -9 option for forcefully terminate a running process by its PID.
# tar -cvf -> tar command with the -cvf options for create a new archive file in verbose mode.
# tar -xvf -> tar command with the -xvf options for extract files from an archive file in verbose mode.
# gzip -k -> gzip command with the -k option for keep the original file after compressing it.
# gunzip -k -> gunzip command with the -k option for keep the original file after decompressing it.
# zip -r -> zip command with the -r option for create a compressed archive file in ZIP format recursively, including all files and directories.
# unzip -l -> unzip command with the -l option for list the contents of a ZIP archive without extracting it